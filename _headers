# GitHub Pages 安全响应头（本文件须位于站点根目录，与 index.html 同级）
# 注意：frame-ancestors 与 X-Frame-Options 只能通过 HTTP 头生效，写进 <meta> 是无效的，
#      所以即使两个页面里已有同一条 CSP，这个文件仍然必须部署。
#      头与 meta 同时存在时浏览器取交集，因此下面这条必须与页面内 CSP 逐指令一致，
#      唯一例外是 frame-ancestors（只有头生效，页面 meta 里没有它，属预期差异）。

/*
  Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self' https://tbrndvwxwvtvyjpsqnuj.supabase.co; object-src 'none'; base-uri 'self'; form-action 'none'; frame-ancestors 'none'; upgrade-insecure-requests
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), camera=(), microphone=(), payment=()
  Cross-Origin-Opener-Policy: same-origin
  Strict-Transport-Security: max-age=31536000; includeSubDomains

/assets/*
  Content-Type: application/javascript; charset=utf-8
  Cache-Control: public, max-age=600

/vendor/*
  Cache-Control: public, max-age=604800, immutable
